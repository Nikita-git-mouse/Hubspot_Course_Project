import React from 'react';
import Pairs from "../components/Pairs";

const Workmaterials = () => {
    return (
        <div>
            <Pairs/>
        </div>
    );
};

export default Workmaterials;